import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
  dateItemBtn: {
    borderWidth: 1,
    paddingHorizontal: 24,
    paddingVertical: 8,
    borderRadius: 8,
    alignItems: 'center'
  },
  numberDay: { fontSize: 24, fontWeight: '600' }
})
