import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
  scheduleBtn: {
    paddingVertical: 8,
    paddingHorizontal: 16,
    borderWidth: 1,
    borderRadius: 8
  }
})
